/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "fun-might-beauty.s3.amazonaws.com",
      "d2ipecq96u0enm.cloudfront.net",
    ], 
  },
};

export default nextConfig;
